import 'package:flutter/material.dart';

class DetailedRecipeScreen extends StatelessWidget {
  const DetailedRecipeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
